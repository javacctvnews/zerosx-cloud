<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zerosx.system.mapper.ISysDictDataMapper">

    <resultMap id="BaseResultMap" type="com.zerosx.system.entity.SysDictData">
        <id column="id" property="id"/>
        <result column="dict_sort" property="dictSort"/>
        <result column="dict_label" property="dictLabel"/>
        <result column="dict_value" property="dictValue"/>
        <result column="dict_type" property="dictType"/>
        <result column="is_default" property="isDefault"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="remarks" property="remarks"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectDictDataList" parameterType="com.zerosx.api.system.dto.SysDictDataRetrieveDTO" resultType="com.zerosx.system.vo.SysDictDataVO">
        select * from t_sys_dict_data
        where 1=1 and deleted = 0
        <where>
            <if test="dictType != null and dictType != ''">
                AND dict_type = #{dictType}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="getSysDictDataSelectList" parameterType="com.zerosx.system.dto.SysDictDataDTO" resultType="com.zerosx.api.system.vo.I18nSelectOptionVO">
        select dict_label AS label,
               dict_value AS value ,
        concat(dict_type,'.',dict_value) as i18nCode,
        css_class as css_class,
        list_class as list_class,
        remarks
        from t_sys_dict_data
        where 1=1 and deleted = 0 and status = '0' and dict_type = #{dictType}
        order by dict_sort asc
    </select>

</mapper>
